<% content_for :title, "Edit Post – #{@post.title}" %>

<div class="flex items-end justify-between mb-6">
  <h1 class="text-3xl font-bold">Edit Post</h1>
  <%= link_to 'Delete', @post, method: :delete, data: { confirm: 'Are you sure you want to delete this post?' }, class: 'rounded-md bg-red-50 px-2.5 py-1.5 text-sm font-semibold text-red-600 shadow-sm hover:bg-red-100' %>
</div>

<%= render 'form', post: @post %>
